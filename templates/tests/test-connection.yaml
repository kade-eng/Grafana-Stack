apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "grafana-stack.fullname" . }}-test-connection"
  labels:
    {{- include "grafana-stack.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "grafana-stack.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
